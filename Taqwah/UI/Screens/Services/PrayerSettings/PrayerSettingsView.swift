import SwiftUI

struct PrayerSettingsView: View {
    var body: some View {
        Text("Prayer Settings")
            .navigationTitle("Prayer Settings")
    }
}

#Preview {
    PrayerSettingsView()
}
